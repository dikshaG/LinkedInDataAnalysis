This folder contains:
Source Code: This folder has all the java code files in which
			 JsonParseCompany contains the main funtion.

Maven jar: Using the above code maven jar is created. This folder
			also comtains pom.xml file used for maven creation.

Output : This folder contains the output when p2.json is taken as
		input for above code files whose path has to be passed as
		command line argument, arg[0].